# Changelog

## v0.2.0 — 2026-09-10

媒体服务器多端适配 + 同步语义重设计（镜像 / 三级删除策略 / 监听同步）+ 进度与日志重做 + 全站 UI 统一。

### 媒体服务器
- 连接容器页重做:一台统一分区 + 选项卡切换 **Emby / Navidrome / Jellyfin / 道理鱼 / Subsonic**
- 「确定」= 保存配置 → 测试连接 → 探测媒体库 → **全部通过才设为同步目标**;分级反馈(连接失败 / 已连接但媒体库未匹配 / 已连接并设为目标),底层报错翻成人话(地址不通 / 鉴权失败 / 404 路径多写)
- Subsonic 无媒体库概念 → 自动跳过探测步骤

### 歌单同步(重设计)
- **镜像模式**:目标播放列表向 LX 看齐(增删同步);原有"完全同步"迁移为 镜像 + 保留文件,行为不漂移
- **LX 端删除的三级处理**:① 仅移除保留文件 ② 移除 + 删文件(先进回收站) ③ 移入归档歌单
- **归档目标 = 一个名字字段**:统一归档 / 按来源(`[歌单名]归档` 占位符)/ 任选已有播放列表,三合一;配置期创建、运行期只找不建(被删则降级为保留文件并标记 `·归档降级`)
- **监听同步**:单监听器 + 模式互斥(完全 / 条件增量);完全模式新增「包含现有歌单」勾选项(显示待建数量、二次确认、取消时重快照基线);忽略列表管理(取消忽略)
- **删除任务确认框**:可勾选"永久忽略该歌单",避免下次扫描重建(修复旧实现只写 autoadd 忽略列表、监听读不到导致"删了又回来"的 bug)
- 所有权追踪(provenance):镜像删除只处理本任务加入过的曲目

### 榜单订阅
- 同步方式可选 **增量 / 镜像跟随榜单**,跌出榜单复用同一套三级删除策略 + 归档目标选择器
- 编辑表单重做为 DaisyUI(原为 Pico 时代裸 markup);列表改为与歌单同步同构的表格
- 榜单拉取为空(而非真空)时判定为源异常 → 停用任务、绝不清空目标

### 进度与历史 / 日志
- 任务进度页实时显示"第几首 / 共几首 · 当前阶段 · 当前歌曲 · 成败计数",1 秒刷新;跑完的结果转入历史
- 「任务进度 + 历史记录」合并为**进度历史**:顶部实时面板 + 歌单同步 / 榜单订阅分标签 + 回收站标签
- 日志**按天落盘** `data/logs/`(重启不丢),界面支持级别筛选与关键字搜索;补齐任务增删启停、配置保存等业务日志
- 设置页「日志保留天数」真正生效(启动时清理过期日志文件)

### 音质校验加固
- 档位补齐 192k;有损档按实测码率校验(320k≥240 / 192k≥150 / 128k≥100kbps),不达标自动降档
- Atmos 系容器嗅探:MP3 换壳拒收;合法容器收下并标注"无法可靠验真"
- 修复引擎"降级后仍按原档校验"的盲区(仅对勾选链内的档降级)

### 界面
- 统一三层视觉:绿底标题条 → 浅灰一级卡片 → 白色内层框(歌单同步 / 榜单订阅 / 连接容器)
- 主题绿统一(填充 / 文字 / 悬停),绿块内文字统一白字
- 修掉 pico 桥残留的三处布局陷阱:按钮、表单控件的纵向 margin 导致 flex 行错位(8px)
- 侧栏:「任务进度」并入进度历史、曲库管理暂时收起、帮助链接移除;下载选项收编批量下载保护

### 其他
- 隐藏未开发/无效项:通知(预留)、高级设置、通用页三个无效开关(自动纳入新歌单 / 清理孤立文件 / 暂停所有同步)
- 移除同步重设计原型页 `/sync-designer`(已并入正式页)

## v0.1.0 — 2026-09-05（首版发布）

首个公开版本：LX 歌单自动下载入库 Emby + 曲库管理。

### 歌单同步
- 增量 / 完全（Emby 向 LX 看齐）两种同步模式，每歌单独立 cron 定时
- 8 档音质链（128k → … → flac24bit / Hi-Res / Atmos / 臻品母带），按实际可用自动尝试
- 真实位深 / 采样率校验（FLAC 头解析 + MP3 帧扫描），防 24bit 虚标与格式换壳
- 完整标签写入（TITLE/歌手/专辑）+ 内嵌 LYRICS + 外置 .lrc 双份歌词
- 全局单飞 + 批量下载保护（请求节流）防音源限流
- 自动新增同步任务：基线快照 + 忽略列表，新歌单自动建任务

### 曲库管理
- **安全洗版**：低码率扫描（阈值可选）→ 190 分匹配算法找候选 → 下载高音质到独立目录，旧文件零触碰；旧版/新版实时探测展示
- **查重清理**：同名同歌手查重（各库独立 / 跨库合并），按音质门槛自动清理或逐条处理，⭐ 保留建议 + 手动扫库
- **回收站安全缓冲**：删除 = 移入 `.songferry-trash` 可恢复，彻底删除才物理删
- **路径映射体检**：媒体库"在映射目录内可回收 / 回收受限"标注，防误删

### 历史与运维
- 同步批次 / 洗版 / 查重三类历史完整记录，失败可重试，查重历史分页翻查 + 行内展开
- 账号认证（scrypt 密码哈希 + 持久会话），Docker env 首启注入
- 路径自检 / 媒体库探测 / 运行日志页
